#include<stdio.h> //! Standard input output

int main(void) { //!ฟังก์ชัน main() คือ ฟังก์ชันพิเศษ กลุ่มคำสั่งที่อยู่ในฟังก์ชันนี้ จะทำงานอัตโนมัติในตอนที่เริ่มต้นเสมอ
    printf("Hello Martin"); //? ขอบเขต(Block) ใช้สัญลักษณ์ {}  เพื่อบอกขอบเขตของการทำงาน

    return (0); //* เครื่องหมายสิ้นสุดคำสั่ง ;
    //! comment ใช้สัญลักษณ์ // or /**/
}